export enum ColumnType {
  String = "string",
  Integer = "integer",
  Float = "float",
  Boolean = "boolean",
  Email = "email",
  Url = "url",
  Ip = "ip",
  Datetime = "datetime",
  Enum = "enum",
}

export enum IndexType {
  Key = "key",
  Unique = "unique",
  FullText = "fulltext",
}
